#include<stdio.h>
#include<math.h>

//to find Area of Square

int main(){
float area, side;
printf ("Enter side Of Square =");
scanf ("%f", &side);
area = side*side;
printf ("Area is = %f \n", area);
return 0;
}
